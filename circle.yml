machine:
    python:
        version: 2.7.8

    environment:
        PYTHONPATH: .

dependencies:
    pre:
        - pip install pytest coverage pytest-cov


test:
    override:
        - make full-test

